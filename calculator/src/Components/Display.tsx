import React from "react";
import { Box, Typography } from "@mui/material";

const Display = () => {
  return (
    <Box sx={{ backgroundColor: "gray", textAlign: "right" }}>
      <Typography sx={{ color: "white" }}>80085</Typography>
    </Box>
  );
};

export default Display;
